{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "version": 2,
  "framework": "nextjs",
  "installCommand": "pnpm --version && pnpm install --frozen-lockfile --verbose",
  "buildCommand": "pnpm migrate && pnpm build"
}
